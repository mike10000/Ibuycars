[build]
  publish = "."
  functions = "netlify/functions"
  command = "echo 'No build step needed'"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[build.environment]
  PYTHON_VERSION = "3.12"

